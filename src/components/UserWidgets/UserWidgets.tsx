// @flow
import * as React from 'react';
import {PropsWithChildren} from "react";

type Props = {

};
export const UserWidgets = (props: PropsWithChildren<Props>) => {
    return (
        <div>

        </div>
    );
};