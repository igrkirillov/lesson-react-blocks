// @flow
import * as React from 'react';

type Props = {
    /**
     * Наименование
     */
    name: string
};
export const UserWidget = (props: Props) => {
    return (
        <div>

        </div>
    );
};