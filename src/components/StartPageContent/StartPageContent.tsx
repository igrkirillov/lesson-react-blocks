// @flow
import * as React from 'react';
import {PropsWithChildren} from "react";

type Props = {

};
export const StartPageContent = (props: PropsWithChildren) => {
    return (
        <div>

        </div>
    );
};