// @flow
import * as React from 'react';
import {PropsWithChildren} from "react";

type Props = {

};
export const YandexApps = (props: PropsWithChildren<Props>) => {
    return (
        <div>

        </div>
    );
};