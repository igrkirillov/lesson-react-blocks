// @flow
import * as React from 'react';

type Props = {
    /**
     * Наименование категории новостей
     */
    category: string
};
export const NewsCategory = (props: Props) => {
    return (
        <div>

        </div>
    );
};