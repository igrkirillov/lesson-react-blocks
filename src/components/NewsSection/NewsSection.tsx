// @flow
import * as React from 'react';
import {PropsWithChildren} from "react";

type Props = {
};

export function NewsSection(props: PropsWithChildren<Props>) {
    return (
        <div>

        </div>
    );
};